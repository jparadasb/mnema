# Offline installation

Offline bundles are planned, not implemented. Current installer requires preinstalled Docker and may use Debian package repositories for host utilities. Never claim air-gapped support for this milestone.
